# Compatibility

Requires Claude Code >= 2.1.110 (version-constrained plugin dependencies —
`sdd-workflow` depends on `shared-skills` with a semver range, which needs
this or a later CLI to resolve and enforce).
