---
name: "an unrelated request never activates the SDD workflow (negative eval)"
tags: ["negative", "activation"]
plugins: ["sdd-workflow"]
runs: 3
max_turns: 8
timeout_seconds: 180
---

Explain, in plain terms, the difference between `git rebase` and
`git merge`, and when you'd reach for each one. No code changes needed —
just explain it.
