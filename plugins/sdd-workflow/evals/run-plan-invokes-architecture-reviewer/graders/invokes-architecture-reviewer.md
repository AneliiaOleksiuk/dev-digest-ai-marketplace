---
type: tool_used
tool: Agent
input_match: "(architecture-reviewer:)?architecture-reviewer"
min: 1
target: trace
---

Phase 3b runs only when `architecture-reviewer` is installed — it is here
— and must be invoked (foreground) against the same diff `implementer`
produced, per `run-plan/SKILL.md`'s Phase 3b.
